version: "2"

linters:
  settings:
    errcheck:
      # Осознанно игнорируем ошибку Close() у читающих ресурсов
      # (resp.Body, sql.Rows): обрабатывать её обычно нечем.
      exclude-functions:
        - (io.Closer).Close
        - (*database/sql.Rows).Close
